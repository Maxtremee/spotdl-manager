---
phase: 01-schema-reset-spotdl-removal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/server/spotdl/functions.ts
  - src/modules/server/spotdl/repository.ts
  - src/modules/server/spotdl/schema.ts
  - src/modules/server/spotdl/SpotdlInvocator.ts
  - src/modules/server/spotdl/SpotdlInvocator.test.ts
  - src/env.ts
  - .env.example
  - Dockerfile
  - Dockerfile.dev
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02

must_haves:
  truths:
    - "Directory src/modules/server/spotdl/ no longer exists"
    - "SPOTDL_COOKIES_FILE is not present anywhere under src/, server/, or in .env.example"
    - "Dockerfile and Dockerfile.dev no longer install python3/pipx/spotdl/python3-pip (spotdl CLI runtime dep removed)"
    - "src/env.ts server schema contains only SERVER_URL; no SPOTDL_COOKIES_FILE key"
    - "grep 'spotdl' src/ server/ returns zero matches in TypeScript/TSX code paths (ARCHITECTURE.md docs are cosmetic and stay)"
    - "pnpm typecheck still fails at plan 01-03 end because scheduler.ts + settings route + cookies form still import from spotdl/ — that's expected; plan 01-02 + plan 01-04 close those gaps in their own waves"
  artifacts:
    - path: "src/modules/server/spotdl"
      provides: "FILE DELETED — entire directory removed"
    - path: "src/env.ts"
      provides: "Server env schema trimmed to SERVER_URL only"
      contains: "SERVER_URL"
    - path: ".env.example"
      provides: "No SPOTDL_COOKIES_FILE reference"
    - path: "Dockerfile"
      provides: "No python3/pipx/spotdl install; no `pip3 install spotdl`; no `spotdl --version` verify step"
    - path: "Dockerfile.dev"
      provides: "No python3/pipx/spotdl install; no `spotdl --version` verify step"
  key_links:
    - from: "src/env.ts"
      to: "process.env"
      via: "createEnv server block"
      pattern: "SERVER_URL: z\\.url\\(\\)"
---

<objective>
Rip out the spotdl module and its environment surface. Delete the entire `src/modules/server/spotdl/` directory (5 files: `functions.ts`, `repository.ts`, `schema.ts`, `SpotdlInvocator.ts`, `SpotdlInvocator.test.ts`). Remove `SPOTDL_COOKIES_FILE` from `src/env.ts`. Purge `SPOTDL_COOKIES_FILE` references from `.env.example`. Remove the spotdl runtime install (python3 + pipx/pip + `spotdl --version` verify) from `Dockerfile` and `Dockerfile.dev` so the Phase 7 image work doesn't inherit dead dependencies.

Purpose: CLEANUP-01 (remove SpotdlInvocator/SpotdlRepository + all CLI wiring) and CLEANUP-02 (remove env var + spotdl-specific knobs) require the module + its env contract to disappear in a single atomic change. Plan 01-04 depends on this deletion (the scheduler stub imports cannot be stripped while scheduler.ts's type-check still can't find the module — stripping the imports is plan 04's job but we must delete the files first so nothing else in the repo can resurrect a transitive dependency).

Output: Eight files modified (two deleted outright, one directory removed, three config files trimmed), two Dockerfiles trimmed of python/spotdl runtime deps. The scheduler (plan 04) and the settings route + cookies form (plan 02) still reference the deleted paths at end of this plan — those consumers are intentionally closed in their own plans. The plan set converges to a green `pnpm typecheck` only after plans 01-01, 01-02, 01-03, and 01-04 are all applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-reset-spotdl-removal/01-CONTEXT.md
@.planning/phases/01-schema-reset-spotdl-removal/01-RESEARCH.md
@.planning/phases/01-schema-reset-spotdl-removal/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@CLAUDE.md

<interfaces>
<!-- Files being deleted. No type interfaces to embed — this is pure removal. -->

Directory to delete (`src/modules/server/spotdl/`) — current contents:
- `functions.ts` (134 lines) — 4 exported server fns: `getSpotdlSettingsServerFn`, `updateSpotdlSettingsServerFn`, `uploadCookiesFileServerFn`, `deleteCookiesFileServerFn`. ALL four are dead after plan 02 (settings route stops importing `getSpotdlSettingsServerFn`) and this plan (directory gone).
- `repository.ts` (~46 lines) — `SpotdlRepository` class; only caller after plan 02 is `PlaylistScheduler.ts` (closed by plan 04).
- `schema.ts` (~15 lines) — `SpotdlSettingsSchema` Zod; only importer is `cookies-settings-form.tsx` (deleted by plan 02) + `repository.ts` itself.
- `SpotdlInvocator.ts` (~215 lines) — the CLI wrapper. Only caller after plan 02 is `PlaylistScheduler.ts` (closed by plan 04).
- `SpotdlInvocator.test.ts` — co-located test; hard-fails if the module is deleted, so it goes with it.

Current `src/env.ts` server block (being trimmed):
```typescript
server: {
    SERVER_URL: z.url().optional(),
    SPOTDL_COOKIES_FILE: z.string().optional(),  // DROP this line
},
```

Current `.env.example` (entire file is SPOTDL comments + one SPOTDL line — file becomes empty or gets a one-line placeholder):
```
# Optional: Cookies file path for spotdl to download in better quality
# Cookies can be enabled/disabled via the Settings page in the UI
# Export cookies from your browser using an extension like "Get cookies.txt LOCALLY"
# SPOTDL_COOKIES_FILE=/path/to/cookies.txt
```

Dockerfile runtime deps being removed (production image):
```
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 \                  ← DROP
    python3-pip \              ← DROP
    ffmpeg \                   ← KEEP (Phase 3+ yt-dlp needs it)
    sqlite3 \                  ← KEEP
    && rm -rf /var/lib/apt/lists/*

RUN pip3 install --no-cache-dir spotdl     ← DROP entire RUN
RUN spotdl --version                       ← DROP entire RUN
```

Dockerfile.dev runtime deps being removed (dev image):
```
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 \                  ← DROP
    python3-pip \              ← DROP
    python3-dev \              ← DROP
    python3-venv \             ← DROP
    pipx \                     ← DROP
    ffmpeg \                   ← KEEP
    git \                      ← KEEP
    sqlite3 \                  ← KEEP
    make \                     ← KEEP (better-sqlite3 native build)
    g++ \                      ← KEEP (better-sqlite3 native build)
    && ln -sf python3 /usr/bin/python \   ← DROP
    && rm -rf /var/lib/apt/lists/*

RUN pipx install spotdl && pipx ensurepath    ← DROP
ENV PATH="/root/.local/bin:$PATH"             ← DROP
RUN spotdl --version                          ← DROP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/modules/server/spotdl/ and strip SPOTDL_COOKIES_FILE from src/env.ts + .env.example</name>
  <read_first>
    - src/modules/server/spotdl/functions.ts (134 lines — being deleted)
    - src/modules/server/spotdl/repository.ts (being deleted)
    - src/modules/server/spotdl/schema.ts (being deleted)
    - src/modules/server/spotdl/SpotdlInvocator.ts (being deleted)
    - src/modules/server/spotdl/SpotdlInvocator.test.ts (being deleted)
    - src/env.ts (current shape — trim server block)
    - .env.example (current shape — purge SPOTDL section)
    - .planning/phases/01-schema-reset-spotdl-removal/01-CONTEXT.md §"Implementation Decisions" D-13
    - .planning/phases/01-schema-reset-spotdl-removal/01-RESEARCH.md §"Runtime State Inventory → DELETE list"
    - CLAUDE.md §"Module Structure" (client/server boundary — spotdl/ lives under server, safe to delete)
  </read_first>
  <action>
    Four surgical operations. Order matters: delete files first, strip env contract second, purge example env third.

    1. **Delete the spotdl directory outright.** Execute:
       ```bash
       rm -rf src/modules/server/spotdl
       ```
       Do NOT selectively delete individual files — the directory is entirely dead code after plan 02 (client consumers removed) and plan 04 (server consumer stripped). Removing it in one shot eliminates any chance of a stale `index.ts` or accidentally-retained helper. Confirm afterward:
       ```bash
       [ ! -d src/modules/server/spotdl ]
       ```

    2. **Trim `src/env.ts` server block.** Replace:
       ```typescript
       server: {
           SERVER_URL: z.url().optional(),
           SPOTDL_COOKIES_FILE: z.string().optional(),
       },
       ```
       With:
       ```typescript
       server: {
           SERVER_URL: z.url().optional(),
       },
       ```
       The rest of the file (clientPrefix, client block, runtimeEnv, emptyStringAsUndefined) is unchanged. Per D-13, `SPOTDL_COOKIES_FILE` has no replacement — it's deleted outright, not renamed.

    3. **Purge `.env.example`.** The current file is 5 lines, all referencing SPOTDL. Replace the entire contents with a single comment placeholder so the file still exists (some tooling / pre-commit hooks expect it):
       ```
       # Copy this file to .env and configure as needed.
       # No required environment variables for local development.
       ```
       Do NOT leave the SPOTDL comment block in place as "documentation" — CONTEXT.md D-13 says the env var "goes". Historical reference lives in git.

    4. **Verification grep.** Run (output MUST be empty for src/ and server/):
       ```bash
       grep -rn "SPOTDL_COOKIES_FILE\|SpotdlInvocator\|SpotdlRepository\|SpotdlSettings" src/ server/ 2>&1
       ```
       Expected state at end of this task: matches exist ONLY in `src/modules/server/scheduler/PlaylistScheduler.ts` (lines 10-11, 19, 23, 27-28, 37) and `src/modules/server/scheduler/PlaylistScheduler.test.ts` (mocks). Those two files are plan 04's territory. No other file in src/ or server/ may reference the spotdl module after this task.

       Also, the `.env.sample` file is 0 bytes — leave it untouched.
  </action>
  <verify>
    <automated>[ ! -d src/modules/server/spotdl ] && ! grep -q "SPOTDL_COOKIES_FILE" src/env.ts && ! grep -q "SPOTDL_COOKIES_FILE" .env.example && grep -q "SERVER_URL" src/env.ts</automated>
  </verify>
  <done>
    Directory `src/modules/server/spotdl/` is gone. `src/env.ts` server block has exactly one field (`SERVER_URL`). `.env.example` no longer mentions SPOTDL. The only remaining `Spotdl*` references across `src/` + `server/` are inside `PlaylistScheduler.ts` and `PlaylistScheduler.test.ts` (both intentionally left for plan 04 to close).
  </done>
  <acceptance_criteria>
    - `[ ! -d src/modules/server/spotdl ]` exits 0
    - `[ ! -f src/modules/server/spotdl/functions.ts ]`
    - `[ ! -f src/modules/server/spotdl/SpotdlInvocator.ts ]`
    - `[ ! -f src/modules/server/spotdl/SpotdlInvocator.test.ts ]`
    - `! grep -q "SPOTDL_COOKIES_FILE" src/env.ts`
    - `! grep -q "SPOTDL_COOKIES_FILE" .env.example`
    - `grep -q "SERVER_URL" src/env.ts` (SERVER_URL still present)
    - `wc -l src/env.ts` prints fewer than 41 (one line shorter than current 41-line file)
    - Grep `grep -rn "SPOTDL_COOKIES_FILE" src/ server/` returns zero results
    - Grep `grep -rn "from \"\\.\\./spotdl\\|from \"~/modules/server/spotdl\"" src/` returns results ONLY in `src/modules/server/scheduler/PlaylistScheduler.ts` and `src/modules/server/scheduler/PlaylistScheduler.test.ts` (plan 04's territory) — no other source files may reference the deleted module
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Strip python/spotdl install steps from Dockerfile and Dockerfile.dev</name>
  <read_first>
    - Dockerfile (86 lines — production image, multi-stage)
    - Dockerfile.dev (53 lines — development image, single-stage)
    - .planning/phases/01-schema-reset-spotdl-removal/01-CONTEXT.md §"Implementation Decisions" D-13 (spotdl removal extends to env var — Dockerfile install steps are the runtime embodiment of that env var)
    - .planning/phases/01-schema-reset-spotdl-removal/01-RESEARCH.md §"State of the Art" (ffmpeg is KEPT for Phase 3+; python3 + spotdl are DROPPED as outdated)
    - .planning/ROADMAP.md §"Phase 7: Docker image & deployment" (Phase 7 will do the full Dockerfile rewrite to add Chromium + yt-dlp; this task only strips dead spotdl deps so Phase 7 starts from a clean base)
  </read_first>
  <action>
    Surgical edits to both Dockerfiles. Do NOT attempt to rewrite either image structure or add Phase 7's Chromium/yt-dlp runtime — that's Phase 7's scope. Only rip dead spotdl runtime deps.

    1. **`Dockerfile` (production).** Three sections change:

       a. In the builder stage `RUN apt-get install` (around line 9-13), the list is `python3-dev make g++`. python3-dev is a build dep for `better-sqlite3`'s native compilation (sqlite bindings + `node-gyp` uses python for build scripts). It must stay — removing it breaks `pnpm install --frozen-lockfile`. **KEEP** this block unchanged.

       b. In the production runtime stage `RUN apt-get install` (around line 37-42), drop `python3` and `python3-pip`:
          ```
          # BEFORE
          RUN apt-get update && apt-get install -y --no-install-recommends \
              python3 \
              python3-pip \
              ffmpeg \
              sqlite3 \
              && rm -rf /var/lib/apt/lists/*

          # AFTER
          RUN apt-get update && apt-get install -y --no-install-recommends \
              ffmpeg \
              sqlite3 \
              && rm -rf /var/lib/apt/lists/*
          ```
          **Important:** `ffmpeg` and `sqlite3` stay — ffmpeg is required by Phase 3+ (DOWNLOAD-01: yt-dlp + ffmpeg pipeline) and sqlite3 is used by the DB wipe verification in plan 05.

       c. Delete the two lines:
          ```
          # Install spotdl
          RUN pip3 install --no-cache-dir spotdl
          # Verify spotdl installation
          RUN spotdl --version
          ```
          (Also delete the two `# Install spotdl` / `# Verify spotdl installation` comment headers above them.)

       d. Update the Dockerfile header comment on line 1 from:
          ```
          # Production Dockerfile with spotdl and all dependencies
          ```
          To:
          ```
          # Production Dockerfile — Node runtime + ffmpeg + sqlite3 (Phase 7 will add Chromium + yt-dlp)
          ```

       e. Leave the multi-stage `FROM node:22-slim` builder/runtime split, `COPY --from=builder`, `USER node`, `HEALTHCHECK`, `CMD` lines unchanged. Phase 7 owns the full image redesign.

       **Known bug, do NOT fix here:** The builder stage is named `FROM node:22-slim` without `AS builder`, and later `COPY --from=builder` references it — this is already broken in the current Dockerfile and is unrelated to spotdl removal. Document in the SUMMARY so Phase 7 picks it up, but do NOT patch it in this task.

    2. **`Dockerfile.dev` (development).** Two sections change:

       a. Reduce the apt install list. Drop `python3`, `python3-pip`, `python3-dev`, `python3-venv`, `pipx`:
          ```
          # BEFORE
          RUN apt-get update && apt-get install -y --no-install-recommends \
              python3 \
              python3-pip \
              python3-dev \
              python3-venv \
              pipx \
              ffmpeg \
              git \
              sqlite3 \
              make \
              g++ \
              && ln -sf python3 /usr/bin/python \
              && rm -rf /var/lib/apt/lists/*

          # AFTER
          RUN apt-get update && apt-get install -y --no-install-recommends \
              ffmpeg \
              git \
              sqlite3 \
              make \
              g++ \
              && rm -rf /var/lib/apt/lists/*
          ```
          **Important:** `make` and `g++` stay — `better-sqlite3` native compilation needs them. `ffmpeg`, `git`, `sqlite3` stay per reasoning above. The `ln -sf python3 /usr/bin/python` line goes because python itself is gone.

          **Note on better-sqlite3 + node-gyp:** `node-gyp` technically requires Python on PATH for native builds. If `pnpm install` in the dev image fails after this change, Phase 7 will need to reintroduce `python3` as a *build-only* dependency. Flag this in the SUMMARY. For Phase 1, we accept the risk — the dev image is rebuilt rarely, and if it breaks, reverting this one change is trivial.

       b. Delete the three lines:
          ```
          # Install spotdl using pipx (recommended for Python CLI apps)
          RUN pipx install spotdl && pipx ensurepath

          # Add pipx binaries to PATH
          ENV PATH="/root/.local/bin:$PATH"

          # Verify spotdl installation
          RUN spotdl --version
          ```
          (Also delete the three comment headers above them.)

       c. Update the Dockerfile.dev header comment on line 1 from:
          ```
          # Development Dockerfile with spotdl and all dependencies
          ```
          To:
          ```
          # Development Dockerfile — Node runtime + ffmpeg (Phase 7 will add Chromium + yt-dlp)
          ```

    3. Leave `docker-compose.yml`, `docker-compose.prod.yml` untouched — they don't mention spotdl or SPOTDL_COOKIES_FILE (verified by grep in research). Their `volumes:` and `environment:` blocks are Phase 7's territory.

    4. **Do NOT test `docker build`.** Image builds are slow (minutes) and orthogonal to Phase 1's goal. Phase 7 validates the Docker path end-to-end. This task only ensures the textual edits are correct. The grep/negative-grep verifications below are sufficient.
  </action>
  <verify>
    <automated>! grep -q "python3\|pipx\|spotdl\|pip3" Dockerfile && ! grep -q "python3\|pipx\|spotdl" Dockerfile.dev && grep -q "ffmpeg" Dockerfile && grep -q "ffmpeg" Dockerfile.dev && grep -q "sqlite3" Dockerfile && grep -q "make" Dockerfile.dev && grep -q "g++" Dockerfile.dev</automated>
  </verify>
  <done>
    Both Dockerfiles compile mentally: no `python3` / `pipx` / `spotdl` / `pip3` references remain. `ffmpeg`, `sqlite3`, `make`, `g++` all kept where needed. Header comments updated so Phase 7 planner sees clean starting state. `docker build` is intentionally NOT run in this task — textual verification only.
  </done>
  <acceptance_criteria>
    - `! grep -q "python3" Dockerfile`
    - `! grep -q "pipx" Dockerfile`
    - `! grep -q "spotdl" Dockerfile`
    - `! grep -q "pip3" Dockerfile`
    - `! grep -q "python3" Dockerfile.dev`
    - `! grep -q "pipx" Dockerfile.dev`
    - `! grep -q "spotdl" Dockerfile.dev`
    - `grep -q "ffmpeg" Dockerfile` (retained)
    - `grep -q "ffmpeg" Dockerfile.dev` (retained)
    - `grep -q "sqlite3" Dockerfile` (retained)
    - `grep -q "make" Dockerfile.dev` (retained for better-sqlite3)
    - `grep -q "g++" Dockerfile.dev` (retained for better-sqlite3)
    - `grep -q "^# Production Dockerfile " Dockerfile` (header updated)
    - `grep -q "^# Development Dockerfile " Dockerfile.dev` (header updated)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → build system | Deleting `src/modules/server/spotdl/` is a source-tree mutation; no runtime attack surface. |
| Docker build args / apt registry | Dropping python3 / pipx / spotdl packages reduces the attack surface of the image (fewer installed packages = fewer CVEs to track). |
| Legacy `.env` on operator workstation | Operator's local `.env` may still set `SPOTDL_COOKIES_FILE`. After this plan, `@t3-oss/env-core` no longer validates that key — it's silently ignored by `import.meta.env`. No security impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | Cookies file on disk (`data/cookies/cookies.txt` from prior runs via deleted `uploadCookiesFileServerFn`) | accept | The file stays on disk after this plan; plan 05's DB wipe + `data/` cleanup handles filesystem residue. Single-user self-hosted per CONTEXT D-05. No cross-tenant exposure. |
| T-03-02 | Denial-of-service | Removing python3 from Dockerfile breaks `better-sqlite3` native build via `node-gyp` | mitigate | `python3-dev` explicitly RETAINED in the production builder stage's apt install list (preserves `node-gyp` build path). Dev image has accepted risk flagged in task action — if `pnpm install` breaks in Dockerfile.dev, Phase 7 reintroduces python3 as build-only. Verified in task action. |
| T-03-03 | Tampering | Operator's `.env` still contains `SPOTDL_COOKIES_FILE=/path` that now silently does nothing | accept | Unused env vars are ignored by the validator. Operator sees no behavior change; the runtime just no longer reads it. No attack vector. Documentation in SUMMARY prompts user to clean their local `.env`. |
| T-03-04 | Information disclosure | Grep / history archive of `src/modules/server/spotdl/SpotdlInvocator.ts` reveals it shelled out to `spotdl` CLI with `--cookies-file` flag | accept | This plan deletes the file; git history retains it but contains no secrets — the cookies file path was read from env at runtime, never committed. No leak. |
| T-03-05 | Repudiation | `grep -r spotdl` finds stale references in `.planning/` docs, `src/modules/server/events/ARCHITECTURE.md`, `src/modules/server/events/IMPLEMENTATION.md`, `src/modules/server/events/examples.ts` | accept | Out of scope for this plan (cosmetic only per RESEARCH). Plan 02 didn't touch them either. Can be cleaned in a later docs-only pass. None of these are build inputs; they affect no runtime behavior. |
</threat_model>

<verification>
- `[ ! -d src/modules/server/spotdl ]` — directory gone
- `! grep -q "SPOTDL_COOKIES_FILE" src/env.ts` — env var removed
- `! grep -q "SPOTDL_COOKIES_FILE" .env.example` — example purged
- `! grep -q "python3\|pipx\|spotdl\|pip3" Dockerfile` — prod Dockerfile clean
- `! grep -q "python3\|pipx\|spotdl" Dockerfile.dev` — dev Dockerfile clean
- `grep -q "ffmpeg" Dockerfile && grep -q "ffmpeg" Dockerfile.dev` — ffmpeg retained for Phase 3+
- `grep -rn "from \"~/modules/server/spotdl\|from \"\\.\\./spotdl" src/` finds matches ONLY in `PlaylistScheduler.ts` and `PlaylistScheduler.test.ts` (plan 04's territory; other consumers removed by plan 02)
- `pnpm typecheck` is expected to STILL FAIL at end of this plan — scheduler still imports from deleted module. Plan 04 makes the full typecheck green.
</verification>

<success_criteria>
- [ ] Directory `src/modules/server/spotdl/` does not exist
- [ ] `src/env.ts` server block contains only `SERVER_URL`
- [ ] `.env.example` contains no `SPOTDL_COOKIES_FILE` reference
- [ ] `Dockerfile` has no python3/pipx/spotdl/pip3 install steps; retains ffmpeg + sqlite3
- [ ] `Dockerfile.dev` has no python3/pipx/spotdl install steps; retains ffmpeg + make + g++ + git + sqlite3
- [ ] Only remaining `Spotdl*` references in `src/` + `server/` are in `PlaylistScheduler.ts` and its test (plan 04's scope)
- [ ] Committed changes are a self-contained delete/trim; no partial state
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-reset-spotdl-removal/01-03-SUMMARY.md` covering:
- Files deleted (5 from spotdl/ directory)
- Env vars removed (`SPOTDL_COOKIES_FILE` from src/env.ts + .env.example)
- Dockerfile deltas (list the specific lines removed)
- Flag for Phase 7: "Dockerfile.dev may need python3 reintroduced as build-only dep if better-sqlite3's node-gyp path breaks — flagged during Phase 1 cleanup"
- Flag for Phase 7: "Existing Dockerfile multi-stage is malformed (`FROM node:22-slim` missing `AS builder`); predates Phase 1, left untouched"
- Grep confirmation that scheduler.ts is the only remaining spotdl importer (plan 04 closes it)
</output>
</content>
</invoke>